<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta name="description" content="" />
    <link
      rel="stylesheet"
      type="text/css"
      href="../../resources/css/styles.css"
    />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65"
      crossorigin="anonymous"
    />
    
    <style>
      html,
      body {
        height: 100%;
        width: 100%;
        margin: 0;
        background-color: red;
      }
      h1 {
        color: darkgoldenrod;
        font-family: sans-serif;
        font-size: 300%;
      }
      main {
        padding-top:25px; 
        padding-bottom:25px; 
        border-radius: 2em;
        background-color: rgb(79, 7, 7);
        display: flex;
        flex-flow: column;
      }
      a {
        /* for <a></a> you have to change the color in the code :( */
        font-family: sans-serif;
        font-size: 100;
        /*color:*/
      }
      .link {
        color: #ff0000;
      } /* CSS link color (red) */
      .link:hover {
        color: #00ff00;
      } /* CSS link hover (green) */
      a:link {
        color: rgb(255, 89, 12);
        text-decoration: none;
        font-weight: normal;
      }
      a:hover {
        color: rgb(255, 221, 69);
        text-decoration: none;
        font-weight: none;
      }

      header {
        background-color: black;
      }
      div {
        color: darkgoldenrod;
      }
      footer {
        background-color: black;
        display: flex;
        flex-flow: column;
      }
      p {
        color: #ffbc14;
        font-family: sans-serif;
        font-size: 100%;
      }
      .btn {
        border: 2px solid #ffbc14 !important;
        color: rgb(0, 0, 0);
        padding: 14px 28px;
        font-size: 16px;
        cursor: pointer;
        border-radius: 20px;
        background-color: rgb(255, 166, 0) !important;
      }
      .form-control {
        background-color: #ffbc14 !important;
        color: black;
      }
      input:-webkit-autofill,
      textarea:-webkit-autofill,
      select:-webkit-autofill {
        -webkit-box-shadow: 0 0 0 1000px #ffbc14 inset !important;
        -webkit-text-fill-color: #000000 !important;
      }
    </style>
  </head>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <body>

    <ul class="navbar">
      <li <% if (page_name === 'welcome') { %>class="active" <% } %>><a class="bi bi-emoji-smile-fill" href="/welcome">Welcome</a></li>
      <li <% if (page_name === 'home') { %>class="active" <% } %>><a class="bi bi-house-door-fill" href="/home">Home</a></li>
      <li <% if (page_name === 'history') { %>class="active" <% } %>><a class="bi bi-clock-fill" href="/pastVideos">History</a></li>
      <li <% if (page_name === 'profile') { %>class="active" <% } %>><a class="bi bi-person-circle" href="/profile">Profile</a></li>
      <li <% if (page_name === 'logout') { %>class="active" <% } %>><a class="bi bi-door-closed-fill" href="/logout">Logout</a></li>
      <li <% if (page_name === 'login') { %>class="active" <% } %>><a class="bi bi-door-open-fill" href="/login">Login</a></li>
      <%- include ('../partials/search') %>
    </ul>
    
    <style>
      @import url(https://fonts.googleapis.com/css?family=Roboto:400,500,700,300);
      *,
      *:after,
      *:before {
        box-sizing: border-box;
      }

      body {
        background: #14171a;
        font-family: "Roboto", sans-serif;
        padding: 25px;
        text-align: center;
      }

      .navbar {
        background: #33363d;
        display: inline-block;
        padding: 0 15px;
        position: relative;
        border-radius: 1em;
      }
      .navbar li {
        display: table-cell;
        padding: 15px;
        min-width: 75px;
        text-align: center;
      }
      .navbar li a {
        display: block;
        color: #14171a;
        text-decoration: none;
        font-size: 32px;
        transition: ease-in 0.3s;
      }
      .navbar li.active a {
        background-color: rgba(255, 255, 255, 0.199);
        border-width: 2em;
        border-color: white;
        border-radius: .2em;
        color: #fff;
        text-shadow: 1px 1px 2px #14171a;
      }
      .navbar li.marker {
        width: calc(100% /6);
        display: inline-block;
        position: absolute;
        height: 5px;
        padding: 0;
        background: #b4a200;
        transition: ease-in 0.3s;
        top: calc(100% - 5px);
        border-radius: 2em;
        left: 15px;
      }
      .navbar li.anim {
        -webkit-animation: 0.4s shake ease-in;
        animation: 0.4s shake ease-in;
      }
      @-webkit-keyframes shake {
        0% {
          transform: translateX(5px);
        }
        50% {
          transform: translateX(-5px);
        }
        100% {
          transform: translateX(0px);
        }
      }
      @keyframes shake {
        0% {
          transform: translateX(5px);
        }
        50% {
          transform: translateX(-5px);
        }
        100% {
          transform: translateX(0px);
        }
      }
    </style>
    <script>
      var navBar = $(".navbar");
      navBar.append('<li class="marker"></li>');

      navBar.on("click", "a", function () {
        var navTap = $(this).closest(".navbar");
        var mrkWidth = $(this).parent("li").width();
        var marker = navTap.find(".marker");
        var nx = $(".navbar").offset();
        var lx = $(".active").offset();
        console.log(lx)
        var left = lx.left - nx.left;

        navBar.find("li").removeClass("active");

        $(this).parent().addClass("active");

        marker.removeClass("anim").css({ width: mrkWidth, left: left });
        setTimeout(function () {
          marker.addClass("anim");
        }, 200);
      });
      navBar.load(".navbar",function() {
        var navTap = $(this).closest(".navbar");
        var mrkWidth = $(this).parent("li").width();
        var marker = navTap.find(".marker");
        var nx = $(".navbar").offset();
        var lx = $(".active").offset();
        console.log(lx)
        var left = lx.left - nx.left;

        

        

<<<<<<< HEAD
<header>
  <nav class="navbar navbar-expand-sm border-bottom">
    <div class="container">
      <button
        class="navbar-toggler ms-auto"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbar-collapse"
        aria-controls="navbar"
        aria-expanded="false"
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbar-collapse">
        <div class="me-auto">
          <h1><a class="nav-link" href="/home">SPOTLIGHT</a></h1>
        </div>
        <div class="nav-item me-auto">
          <a class="nav-link" href="/welcome">Welcome</a>
        </div>
        <div class="nav-item me-auto">
          <a class="nav-link" href="/pastVideos">Past Videos</a>
        </div>
        <div class="nav-item me-auto">
          <a class="nav-link" href="/profile">Profile</a>
        </div>
        <div class="nav-item me-auto">
          <a class="nav-link" href="/logout">Logout</a>
        </div>
        <div class="nav-item me-auto">
          <a class="nav-link" href="/login">Login(Temporary)</a>
        </div>
    </div>
  </nav>
  </header> 
=======
        marker.removeClass("anim").css({ width: mrkWidth, left: left });
        setTimeout(function () {
          marker.addClass("anim");
        }, 200);
      })
    </script>
    
  </body>
</html>
>>>>>>> 693001bdc4e1cc4cca413d69bd77a0e9d300061a
